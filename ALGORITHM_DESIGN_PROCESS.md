# 🚀 코딩 테스트 알고리즘 설계 및 구현 표준 프로세스 (SOP)

이 문서는 소마(SoMa), 우테코 등 복잡한 요구사항을 가진 시뮬레이션 및 객체지향적 구현 문제를 해결하기 위한 **나만의 표준 사고 및 구현 프로세스**를 정의합니다. '구현 주도형(Data-Structure First)' 사고에서 벗어나, '설계 주도형(Lifecycle First)' 사고로 전환하기 위한 가이드라인입니다.

## 🧠 Part 1. 알고리즘 설계 프로세스 (5 Steps)
코드를 타이핑하기 전, 요구사항을 분석하고 구조를 기획하는 단계입니다.

1. **요구사항 추상화 (Rule Extraction)**
   - 문제의 복잡한 서사를 걷어내고, 게임의 규칙과 객체의 핵심 '행동 단위(Action)'만 한글로 간결하게 요약한다.
2. **생명주기(Lifecycle)와 스코프(Scope) 설계 ⭐️**
   - 데이터가 언제 생성되고 언제 소멸하는지(전체 라운드 유지 vs 단일 매칭 유지) 파악한다.
   - 무거운 글로벌 상태(전역 변수)를 지양하고, 생명주기에 맞는 지역 변수로 스코프를 제한하여 복잡도를 낮춘다.
3. **최적의 자료구조 매칭 및 시간복잡도 계산**
   - 앞서 정의한 로직을 가장 가볍게 담을 수 있는 자료구조를 선택한다. (예: 2차원 배열 대신 1차원 배열의 셔플 및 수학적 인덱스 변환 활용)
4. **예외 케이스(Edge Case) 방어 시나리오 구상**
   - 홀수 매칭, 특정 그룹 전멸, 0으로 나누기 등 치명적인 오류가 날 수 있는 경계값을 미리 식별하고 방어 로직을 구상한다.
5. **블록 단위 모듈화 (수도코드 작성)**
   - 거대한 단일 함수(Spaghetti Code)를 방지하기 위해, 독립적으로 테스트 가능한 하위 함수(부품)들로 분리하여 서명(Interface)을 기획한다.

## 💻 Part 2. 실전 구현 프로세스 (4 Phases)
설계도를 바탕으로 빠르고 버그 없이 코드를 조립하는 단계입니다.

1. **스켈레톤(뼈대) 코드 세우기 (Top-Down)**
   - 로직을 채우기 전, 설계한 함수들의 이름과 매개변수, 반환값 형태만 껍데기(`pass`)로 만들어둔다.
2. **독립적인 하위 모듈 완성 (Bottom-Up)**
   - 가장 작고 독립적인 톱니바퀴(예: 단일 매칭 게임 진행 함수)부터 내부 로직을 구현하고, 임의의 값을 넣어 완벽히 동작하는지 검증한다.
3. **메인 로직 조립 및 상태 업데이트**
   - 검증된 하위 모듈들을 `solution` 메인 함수 안에서 호출하여 전체 시뮬레이션 흐름(예: T라운드 반복)을 완성한다.
4. **드라이 런(Dry Run)과 디버깅**
   - 가장 작은 단위의 테스트 케이스를 바탕으로, 코드 실행 흐름과 변수의 상태 변화를 머릿속(또는 메모)으로 추적하며 논리적 오류를 최종 점검한다.
